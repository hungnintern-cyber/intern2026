## 1. 事前準備
テストを実行するには、`pytest` ライブラリが必要です。
以下のコマンドでインストールしてください。
```bash
pip install -r requirements.txt
```

## 2. テストの実行
以下のコマンドを実行すると、exercises フォルダ内の全テストが走ります。
```bash
python -m pytest exercises
```